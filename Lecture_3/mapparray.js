const finalParticipants = ["Taylor" , "Donald" ,"Don" ,"Natasha" ,"Boddy"] ;

const announcements = finalParticipants.map((member) => {
    return member + " joined the contest." ;

}) ;

console.log (announcements) ;