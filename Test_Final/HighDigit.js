function HighestDigit (numStr) {

    numStr = "" ;

    for (let i = 0 ; i < numStr.toString().length ; i++ ) {
        if (!numStr.include(i.toString())) {
            

        }
         
    }

}

HighestDigit (379) ;
HighestDigit (2) ;
HighestDigit (377401) ;